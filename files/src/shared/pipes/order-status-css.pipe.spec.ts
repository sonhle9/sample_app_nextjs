import {OrderStatusCssPipe} from './order-status-css.pipe';

describe('OrderStatusCssPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderStatusCssPipe();
    expect(pipe).toBeTruthy();
  });
});
