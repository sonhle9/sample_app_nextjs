import {TxnStatusCssPipe} from './txn-status-css.pipe';

describe('TxnStatusCssPipe', () => {
  it('create an instance', () => {
    const pipe = new TxnStatusCssPipe();
    expect(pipe).toBeTruthy();
  });
});
