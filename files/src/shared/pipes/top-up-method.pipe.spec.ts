import {TopUpMethodPipe} from './top-up-method.pipe';

describe('TopUpMethodPipe', () => {
  it('create an instance', () => {
    const pipe = new TopUpMethodPipe();
    expect(pipe).toBeTruthy();
  });
});
