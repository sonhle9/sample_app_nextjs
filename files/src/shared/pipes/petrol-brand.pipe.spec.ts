import {PetrolBrandPipe} from './petrol-brand.pipe';

describe('PetrolBrandPipe', () => {
  it('create an instance', () => {
    const pipe = new PetrolBrandPipe();
    expect(pipe).toBeTruthy();
  });
});
