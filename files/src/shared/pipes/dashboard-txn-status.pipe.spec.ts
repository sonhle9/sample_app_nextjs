import {DashboardTxnStatusPipe} from './dashboard-txn-status.pipe';

describe('DashboardTxnStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new DashboardTxnStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
