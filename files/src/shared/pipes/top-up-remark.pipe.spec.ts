import {TopUpRemarkPipe} from './top-up-remark.pipe';

describe('TopUpRemarkPipe', () => {
  it('create an instance', () => {
    const pipe = new TopUpRemarkPipe();
    expect(pipe).toBeTruthy();
  });
});
