import {StationStatusPipe} from './station-status.pipe';

describe('StationStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new StationStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
