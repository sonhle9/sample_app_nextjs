import {StoreOrderStatusCssPipe} from './store-order-status-css.pipe';

describe('StoreOrderStatusCssPipe', () => {
  it('create an instance', () => {
    const pipe = new StoreOrderStatusCssPipe();
    expect(pipe).toBeTruthy();
  });
});
