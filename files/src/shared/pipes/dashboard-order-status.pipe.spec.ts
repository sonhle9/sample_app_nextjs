import {DashboardOrderStatusPipe} from './dashboard-order-status.pipe';

describe('DashboardOrderStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new DashboardOrderStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
