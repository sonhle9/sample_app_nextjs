import {StoreOrderStatusPipe} from './store-order-status.pipe';

describe('StoreOrderStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new StoreOrderStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
