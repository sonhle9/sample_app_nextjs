import {PaginationCountPipe} from './pagination-count.pipe';

describe('PaginationCountPipe', () => {
  it('create an instance', () => {
    const pipe = new PaginationCountPipe();
    expect(pipe).toBeTruthy();
  });
});
