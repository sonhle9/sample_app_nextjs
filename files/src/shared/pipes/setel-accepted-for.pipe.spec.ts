import {SetelAcceptedForPipe} from './setel-accepted-for.pipe';

describe('SetelAcceptedForPipe', () => {
  it('create an instance', () => {
    const pipe = new SetelAcceptedForPipe();
    expect(pipe).toBeTruthy();
  });
});
